{
  "name": "aggregated-orderbooks",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "cli": "bun run src/cli.ts",
    "build": "bun build src/browser.ts --outfile docs/bundle.js --target=browser --minify",
    "dev": "bun build src/browser.ts --outfile docs/bundle.js --target=browser --watch",
    "serve": "bun run build && bun --eval \"Bun.serve({ port: 3000, fetch(req) { const url = new URL(req.url); const path = url.pathname === '/' ? '/index.html' : url.pathname; return new Response(Bun.file('docs' + path)); } }); console.log('http://localhost:3000')\""
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
